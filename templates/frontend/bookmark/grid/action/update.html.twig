{% import '@SyliusUi/Macro/buttons.html.twig' as buttons %}

{% set routeName = data.type is constant('TYPE_VIDEO', data) ? 'app_frontend_bookmark_update_video' : 'app_frontend_bookmark_update_photo' %}
{% set path = path(routeName, options.link.parameters|default({'id': data.id})) %}

{{ buttons.edit(path, action.label) }}
